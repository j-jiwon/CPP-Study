# 1 "macro.cpp"
# 1 "<built-in>"
# 1 "<command-line>"
# 1 "macro.cpp"


int main()
{
 int num1 = 1;
 int num2 = 2;

 int max1 = (((num1) > (num2)) ? (num1) : (num2));
 int max2 = (((num1) > (num2)) ? (num1) : (num2));
 return 0;
}
